services:
  backend:
    build:
      context: ./WebApp/GeneratorService
      target: prod
    ports:
      - "5016:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

  frontend:
    build:
      context: ./WebApp/Frontend
      target: prod
    ports:
      - "80:80"
